import { AlertTriangle } from "lucide-react";
const alerts = [{ title: "Critical error burst", sev: "critical", source: "payment-service" },{ title: "SLA degradation", sev: "warning", source: "scheduler" },{ title: "Authentication retries", sev: "warning", source: "gateway" }];
export default function AlertsPage() { return <div className="space-y-4"><h1 className="text-xl font-semibold">Alerts</h1>{alerts.map((a) => <div key={a.title} className="glass-card animate-slide-up rounded-lg p-5"><div className="flex items-center gap-3"><AlertTriangle className="h-4 w-4" /><span className="font-medium">{a.title}</span><span className={`status-dot ${a.sev === "critical" ? "status-dot-critical" : "status-dot-warning"} animate-pulse-glow`} /></div><p className="mt-2 text-sm text-muted-foreground">Source: {a.source}</p></div>)}</div>; }
